<UserControl x:Class="MacOsUsbSetup.App.Views.UsbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Headline}" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="{Binding Warning}" Foreground="{StaticResource Badge.Experimental}" Margin="0,8,0,16" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasDisks, Converter={StaticResource BoolToVisible}}">
            <ItemsControl ItemsSource="{Binding Disks}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource Card}" Command="{Binding SelectCommand}" Width="370" Margin="0,0,12,12">
                            <StackPanel>
                                <TextBlock Text="{Binding Model}" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Details}" Foreground="{StaticResource Text.Muted}" Margin="0,2,0,0" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="{Binding EmptyNotice}" Foreground="{StaticResource Text.Muted}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="480"
                   Visibility="{Binding HasDisks, Converter={StaticResource BoolToCollapsed}}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Style="{StaticResource Ghost}" Content="Zurück" Command="{Binding BackCommand}" Margin="0,0,10,0" />
            <Button Style="{StaticResource Ghost}" Content="Aktualisieren" Command="{Binding RefreshCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
