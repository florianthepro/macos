<UserControl x:Class="MacOsUsbSetup.App.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="560">
            <TextBlock Text="{Binding Headline}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding Caption}" Foreground="{StaticResource Text.Muted}"
                       HorizontalAlignment="Center" Margin="0,6,0,20" />
            <ProgressBar Style="{StaticResource Bar}" IsIndeterminate="{Binding IsScanning}" Width="440" />
            <Border Style="{StaticResource LogBox}" Height="160" Margin="0,20,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                    <ItemsControl ItemsSource="{Binding Log}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Style="{StaticResource LogLine}" Text="{Binding}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
