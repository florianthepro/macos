<UserControl x:Class="MacOsUsbSetup.App.Views.VersionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Headline}" FontSize="18" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="{Binding HardwareSummary}"
                   Foreground="{StaticResource Text.Muted}" Margin="0,6,0,16" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding HasCompatible, Converter={StaticResource BoolToVisible}}">
            <ItemsControl ItemsSource="{Binding Tiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource Card}" Command="{Binding SelectCommand}" Width="250" Margin="0,0,12,12">
                            <StackPanel>
                                <DockPanel LastChildFill="True">
                                    <Border x:Name="badge" DockPanel.Dock="Right" CornerRadius="3" Padding="6,2"
                                            VerticalAlignment="Top" Background="{StaticResource Badge.Recommended}">
                                        <TextBlock Text="{Binding Badge}" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="#141414" TextWrapping="NoWrap" />
                                    </Border>
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                                </DockPanel>
                                <TextBlock Text="{Binding Version}" Foreground="{StaticResource Text.Muted}" Margin="0,2,0,0" />
                                <TextBlock Text="{Binding Notes}" FontSize="11" Foreground="{StaticResource Text.Muted}"
                                           Margin="0,10,0,0"
                                           Visibility="{Binding HasNotes, Converter={StaticResource BoolToVisible}}" />
                            </StackPanel>
                        </Button>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsExperimental}" Value="True">
                                <Setter TargetName="badge" Property="Background" Value="{StaticResource Badge.Experimental}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="{Binding EmptyNotice}" Foreground="{StaticResource Text.Muted}"
                   VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="480"
                   Visibility="{Binding HasCompatible, Converter={StaticResource BoolToCollapsed}}" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Style="{StaticResource Ghost}" Content="Zurück" Command="{Binding BackCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
