<UserControl x:Class="MacOsUsbSetup.App.Views.DoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Headline}" FontSize="18" FontWeight="SemiBold"
                   Foreground="{StaticResource Badge.Recommended}" />
        <TextBlock Grid.Row="1" Text="{Binding Summary}" Margin="0,6,0,16" />

        <Border Grid.Row="2" Style="{StaticResource LogBox}" Padding="16" VerticalAlignment="Top">
            <ItemsControl ItemsSource="{Binding Steps}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,3" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Style="{StaticResource Primary}" Content="Rechner neu starten" Command="{Binding RebootCommand}" Margin="0,0,10,0" />
            <Button Style="{StaticResource Ghost}" Content="Protokoll anzeigen" Command="{Binding ShowLogCommand}" Margin="0,0,10,0" />
            <Button Style="{StaticResource Ghost}" Content="Neuen USB erstellen" Command="{Binding RestartCommand}" Margin="0,0,10,0" />
            <Button Style="{StaticResource Ghost}" Content="Beenden" Command="{Binding QuitCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
