<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mvvm="clr-namespace:MacOsUsbSetup.App.Mvvm">

    <BooleanToVisibilityConverter x:Key="BoolToVisible" />
    <mvvm:InverseBooleanToVisibilityConverter x:Key="BoolToCollapsed" />

    <SolidColorBrush x:Key="Surface.Window" Color="#1E1E1E" />
    <SolidColorBrush x:Key="Surface.Header" Color="#252526" />
    <SolidColorBrush x:Key="Surface.Panel" Color="#252526" />
    <SolidColorBrush x:Key="Surface.PanelHover" Color="#2D2D30" />
    <SolidColorBrush x:Key="Surface.Border" Color="#3F3F46" />
    <SolidColorBrush x:Key="Text.Primary" Color="#E6E6E6" />
    <SolidColorBrush x:Key="Text.Muted" Color="#9DA0A4" />
    <SolidColorBrush x:Key="Accent" Color="#3794FF" />
    <SolidColorBrush x:Key="Accent.Hover" Color="#4A9FFF" />
    <SolidColorBrush x:Key="Accent.Pressed" Color="#2A7AD0" />
    <SolidColorBrush x:Key="Badge.Recommended" Color="#4EC94E" />
    <SolidColorBrush x:Key="Badge.Experimental" Color="#E5A03A" />
    <SolidColorBrush x:Key="Danger" Color="#E05555" />

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="Primary" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" CornerRadius="3" Background="{StaticResource Accent}" Padding="16,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Accent.Hover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Accent.Pressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Surface.Border}" />
                            <Setter Property="Foreground" Value="{StaticResource Text.Muted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Ghost" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" CornerRadius="3" Background="Transparent"
                            BorderBrush="{StaticResource Surface.Border}" BorderThickness="1" Padding="16,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Surface.PanelHover}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Text.Muted}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Card" TargetType="Button">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bg" CornerRadius="4" Background="{StaticResource Surface.Panel}"
                            BorderBrush="{StaticResource Surface.Border}" BorderThickness="1" Padding="16,14">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Surface.PanelHover}" />
                            <Setter TargetName="bg" Property="BorderBrush" Value="{StaticResource Accent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bg" Property="Background" Value="{StaticResource Surface.Header}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Bar" TargetType="ProgressBar">
        <Setter Property="Height" Value="6" />
        <Setter Property="Foreground" Value="{StaticResource Accent}" />
        <Setter Property="Background" Value="{StaticResource Surface.Border}" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="LogBox" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Surface.Panel}" />
        <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <Style x:Key="LogLine" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{StaticResource Text.Muted}" />
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>

</ResourceDictionary>
